import { Collection } from "../Collection.js";

export class BaseCollectionManager {
    /**
     * Saves a collection
     * 
     * @param {Collection} collection Collection to add
     */
    addCollection(collection) {
    }

    /**
     * Gets all saved collections
     * 
     * @returns {Collection[]}
     */
    getCollections() {
        return [];
    }

    /**
     * Updates a saved collection
     * 
     * @param {Collection} collection Collection to update
     */
    updateCollection(collection) {
    }

    /**
     * Deletes a saved collection
     * 
     * @param {Collection} collection Collection to delete
     */
    deleteCollection(collection) {
    }
}
